<!-- toc-start -->

# 目录

[1 我给AI写了一套"芯片验证SOP"，它真的帮我抓到了bug](#1-我给ai写了一套芯片验证sop它真的帮我抓到了bug)  
　　[1.1 起因：一个反复折腾人的问题](#11-起因一个反复折腾人的问题)  
　　[1.2 一句话说清楚](#12-一句话说清楚)  
　　[1.3 原理：给AI一个"老员工的操作手册"](#13-原理给ai一个老员工的操作手册)  
　　[1.4 一次真实的Debug：UART TX bit\[0\]翻转](#14-一次真实的debuguart-tx-bit0翻转)  
　　　　[1.4.1 故障现象](#141-故障现象)  
　　　　[1.4.2 AI的分析过程](#142-ai的分析过程)  
　　　　[1.4.3 Verdi波形截图](#143-verdi波形截图)  
　　　　[1.4.4 这个过程说明了什么](#144-这个过程说明了什么)  
　　　　[1.4.5 关于这个case的难度](#145-关于这个case的难度)  
　　[1.5 黑猫白猫，能抓老鼠的就是好猫](#15-黑猫白猫能抓老鼠的就是好猫)  
　　[1.6 关于fsdb\_reader.py](#16-关于fsdb_readerpy)  
　　[1.7 Skill的价值：把"可重复的经验"变成"可执行的知识"](#17-skill的价值把可重复的经验变成可执行的知识)  
　　[1.8 写在最后](#18-写在最后)  
[2 关于控制Agent上下文的一些方法与思考](#2-关于控制agent上下文的一些方法与思考)  
　　[2.1 长对话里 AI 会变笨，这不是错觉](#21-长对话里-ai-会变笨这不是错觉)  
　　[2.2 Compact 不是好答案，Sub-agent 才是](#22-compact-不是好答案sub-agent-才是)  
　　[2.3 轻量版：Claude Code 的 `/btw` 和 Codex 的 `/side`](#23-轻量版claude-code-的-btw-和-codex-的-side)  
　　[2.4 重量版：Claude Code 的 `/fork` 和 Codex 的 sub-agent](#24-重量版claude-code-的-fork-和-codex-的-sub-agent)  
　　　　[2.4.1 Claude Code：`/fork` vs 普通 sub-agent](#241-claude-codefork-vs-普通-sub-agent)  
　　　　[2.4.2 Codex CLI：显式委派 + `/agent` 切换](#242-codex-cli显式委派-agent-切换)  
　　[2.5 为什么这套用法对我个人特别契合](#25-为什么这套用法对我个人特别契合)  
　　[2.6 最后一点感受](#26-最后一点感受)  
[3. AI辅助覆盖率收敛的坑在哪里？](#3-ai辅助覆盖率收敛的坑在哪里)  
　　[3.1 机器学习进来了，先看清它在哪一层干活](#31-机器学习进来了先看清它在哪一层干活)  
　　[3.2 真正能预测盲区的信号，藏在别的地方](#32-真正能预测盲区的信号藏在别的地方)  
　　[3.3 工具变强之后，判断力反而更贵](#33-工具变强之后判断力反而更贵)  
[4. AI 写 UVC？我踩了无数坑后，搞了个 Skill 包救自己](#4-ai-写-uvc我踩了无数坑后搞了个-skill-包救自己)  
　　[4.1 一周没更新，后台炸了](#41-一周没更新后台炸了)  
　　[4.2 第一次尝试：AI 的 UVM，语法都写不对](#42-第一次尝试ai-的-uvm语法都写不对)  
　　[4.3 第一轮突破：给 AI 加上"红绿灯"——TDD](#43-第一轮突破给-ai-加上红绿灯tdd)  
　　[4.4 第二轮突破：Wavedrom——让 AI 正确"看懂"时序](#44-第二轮突破wavedrom让-ai-正确看懂时序)  
　　[4.5 第三轮挑战：Guidelines——让 AI 写出"好"的 UVC](#45-第三轮挑战guidelines让-ai-写出好的-uvc)  
　　　　[4.5.1 问题一：Guideline 写得太抽象，AI 不理解](#451-问题一guideline-写得太抽象ai-不理解)  
　　　　[4.5.2 问题二：模型差异巨大](#452-问题二模型差异巨大)  
　　[4.6 最终方案：代码模板化，脚本 + Prompt 两步走](#46-最终方案代码模板化脚本-prompt-两步走)  
　　[4.7 env-builder：一个完整的 UVC 开发 Skill](#47-env-builder一个完整的-uvc-开发-skill)  
　　　　[4.7.1 uvc\_gen：UVC 框架生成脚本](#471-uvc_genuvc-框架生成脚本)  
　　　　[4.7.2 开发 UVC 的流程](#472-开发-uvc-的流程)  
　　[4.8 ic-verifier：一个面向 DV 的 AI Skill 技能包](#48-ic-verifier一个面向-dv-的-ai-skill-技能包)  
　　[4.9 总结：AI 开发 UVC 的正确姿势](#49-总结ai-开发-uvc-的正确姿势)  
　　　　[4.9.1 TDD 是必须的](#491-tdd-是必须的)  
　　　　[4.9.2 时序描述要结构化](#492-时序描述要结构化)  
　　　　[4.9.3 框架和逻辑分离](#493-框架和逻辑分离)  
　　　　[4.9.4 Guidelines 要具体](#494-guidelines-要具体)  
　　　　[4.9.5 不同模型要区别对待](#495-不同模型要区别对待)  
　　[4.10 项目地址](#410-项目地址)  
[5. AI 会写 testbench 了，验证工程师慌不慌？](#5-ai-会写-testbench-了验证工程师慌不慌)  
　　[5.1 先说清楚 AI 到底吃掉了什么](#51-先说清楚-ai-到底吃掉了什么)  
　　[5.2 验证到底在解什么问题](#52-验证到底在解什么问题)  
　　[5.3 护城河在哪：三样 AI 短期拿不走的东西](#53-护城河在哪三样-ai-短期拿不走的东西)  
　　　　[5.3.1 把模糊的意图翻译成可执行的判断](#531-把模糊的意图翻译成可执行的判断)  
　　　　[5.3.2 判断什么值得验，什么不值得](#532-判断什么值得验什么不值得)  
　　　　[5.3.3 对失败的直觉](#533-对失败的直觉)  
　　[5.4 该往哪走](#54-该往哪走)  
　　[5.5 最后](#55-最后)  

<!-- toc-end -->

---

# 1 我给AI写了一套"芯片验证SOP"，它真的帮我抓到了bug

> 来源：https://mp.weixin.qq.com/s/9qUIKQYhLRh7TGfIjJQRhw
> 作者：AAA建材批发王哥
> update 2026/08/16 01 : 13
> 一个数字前端工程师的AI工具实践记录

---

## 1.1 起因：一个反复折腾人的问题

做数字IC的朋友大概都经历过这种场景——

你接手一个新项目，或者隔了几个月回来维护一个老项目。打开终端，准备跑一把仿真。然后你发现：环境变量忘了source，EDA工具不知道为什么hang住了，flist里少了个define文件，regression跑完了但pass/fail的判定标准和上次不一样……

这些事情单独拎出来都不难。但每次都要重新踩一遍，每次都要翻之前的笔记或者问同事，累积起来就是一种很消耗人的精力。

说句实话，我本职是做设计的。但项目环境起不来、仿真脚本报错、regression结果需要人肉扫一遍——这些事情本来不该占用我的时间，但它们确实会block住我。我需要一个7×24小时待命的资深DV同事，帮我扫平这些障碍。

于是我想，能不能把这些"做过一次就不该再想的事情"真正固化下来——不是写成一篇自己都懒得翻的readme，而是直接让AI知道该怎么做，我说一句"帮我跑个仿真"，它就能按照正确的流程执行。

这就是**eda-sim skill**的由来——一套写给AI的芯片验证工作流指令集。

---

## 1.2 一句话说清楚

它是一份**结构化的操作手册**，但读者不是人——读者是AI。

具体来说，它是Claude Code的一个"Skill"。我把积累下来的VCS/Verdi仿真经验，按照AI能理解和执行的方式，写成了一份详细的SOP。AI读了这份SOP之后，就能在我的指令下帮我完成从编译、仿真、日志分析到波形debug的大部分操作。

它不会帮你写RTL，不会帮你设计testplan，不会替你思考。它就是一个忠实执行你经验的工具人。

---

## 1.3 原理：给AI一个"老员工的操作手册"

很多人对AI的理解还停留在"问它问题，它给你答案"。但现在的AI agent能做的远不止这些——它可以操作你的终端，执行命令，读文件，分析日志。

问题在于，AI不知道你项目的具体情况。它不知道你的VCS装在哪儿，不知道你的license server怎么配的，不知道你们项目的flist结构，不知道你regression的pass/fail判定规则，更不知道`sim.log`里哪些warning是可以忽略的。

**Skill就是解决这个gap的。**

Skill的本质是一份markdown文档（SKILL.md），外加可选的辅助脚本。当你对AI说"帮我跑一下这个case的仿真"时，AI会先读取这份skill文档，了解整个工作流的规范，然后按步骤执行。

拿这个eda-sim skill来说，它告诉AI：

**环境配置。** 每次session开始，先`source .envrc`，把VCS\_HOME、VERDI\_HOME、LICENSE这些全部设好。什么路径该指向哪里，什么环境变量必须导出，哪些wrapper命令不能直接用、必须指向二进制本体——这些全写在skill里。你每次手动配环境要花十分钟回忆的东西，AI读一遍SKILL.md就全知道了。

**编译和仿真。** 优先用原来环境自带的仿真脚本，无论是Makefile还是Python，尽量在此基础上去复现之前的flow，而不是重复造轮子。Skill里同时记录了各种踩坑经验：哪些flag绝对不能用（比如VCS 2023.12之后`+memcbk`已经废弃了），哪些timescale配置会有问题（`-unit_timescale`会覆盖所有模块的timescale，包括vendor IP的）。

**编译错误排查。** 一张常见编译错误的对照表——什么错误对应什么原因，怎么修。AI遇到编译失败时可以对照这张表给出修复建议。

**Regression分析。** 批量分析sim.log，按错误类型归类，区分真正的failure和假fail，给出优先修复建议。

**结果判定。** 不是简单的"有没有error"就完了。sim.log里充满了假fail——比如UVM transaction打印里的`error_type`字段名、analog model的初始化warning、SRAM model在没有SDF的功能仿真里的`MEM_Error`。我把所有常见的误报pattern都写进了skill，AI在分析日志时会自动过滤这些。

**波形debug和coverage。** 怎么开FSDB dump，怎么拉起Verdi，怎么merge coverage。还有一个`fsdb_reader.py`脚本——它能不开GUI直接读FSDB波形数据，AI可以直接查信号值，不需要你手动开Verdi。

---

## 1.4 一次真实的Debug：UART TX bit[0]翻转

光说原理太空洞。下面是一次真实的debug过程——我在OpenTitan的UART TX模块里注入了一个bug，然后让AI来定位它。

### 1.4.1 故障现象

跑完一个UART smoke test，sim.log里scoreboard报了10次TX item mismatch。每次expected和observed的差距都很类似：

```
expected: 0x7e    observed: 0x7f
expected: 0xa4    observed: 0xa5
```

每次恰好差1——bit[0]被翻转了。

### 1.4.2 AI的分析过程

我让AI去查波形。它的做法是：不开Verdi GUI，直接用skill自带的`fsdb_reader.py`脚本去读FSDB文件。

**第一步，列信号。** AI先用`--list-signals`把FSDB里的信号层级扫了一遍，从顶层tb逐步深入到`uart_core`、`uart_tx`模块内部，定位到关键信号的路径。

**第二步，追踪数据路径。** AI沿着数据流方向，逐个观测点读取信号值：

- **观测点A：CPU写WDATA寄存器** — 读`reg_wdata`和`reg_addr`，确认CPU写入的是`0x7F`，地址`0x1C`（WDATA寄存器）。数据在源头是正确的。
- **观测点B：TX FIFO输出** — 读`tx_fifo_data`，值仍然是`0x7F`。FIFO通路没问题。
- **观测点C：TX串口引脚** — 这里是1-bit串行线，AI读取了`uart_tx`引脚和`uart_tx_clk`采样时钟，逐bit解码出实际发送的字节。

**第三步，逐bit解码。** AI用采样时钟的上升沿逐个采样TX引脚：

```
采样时钟上升沿    uart_tx值   含义
─────────────────────────────────
210841199 ps     0           Start bit ✓
315007867 ps     0           D0 = 0  ← 应该是1！
419174535 ps     1           D1 = 1  ✓
523341203 ps     1           D2 = 1  ✓
...
```

解码结果：`0111_1110` = **0x7E**。FIFO给的是0x7F，TX线上发出来的是0x7E。bit[0]被翻转了。

AI又对第二帧做了交叉验证——FIFO输出`0xA5`，TX线解码出`0xA4`，同样是bit[0]翻转。模式完全一致。

**第四步，精确定位bug位置。** AI进一步深入到`uart_tx`模块内部，读取了`wr_data`（模块输入）和`sreg_q`（移位寄存器）：

```
wr_data = 0x7F  (正确)
sreg_q  = 0x6FC (错误！应该是 0x6FE)
```

`sreg_q`是11-bit的移位寄存器：`{stop, parity, data[7:0], start}`。拆解`0x6FC = 11'b110_1111_1100`，其中data部分是`0111_1110 = 0x7E`——数据在从`wr_data`加载到移位寄存器的那一刻就已经被改错了。

**结论：** bug在`uart_tx.sv`的`sreg_d`赋值逻辑里，`wr_data`被执行了`XOR 8'h01`操作（等效于翻转bit[0]）。检查源码第67行，果然在那里。

### 1.4.3 Verdi波形截图

![](AI在芯片中的实际应用_assets/image-0004.png)

![](AI在芯片中的实际应用_assets/image-0005.png)

### 1.4.4 这个过程说明了什么

从"scoreboard报10次mismatch"到"定位到uart\_tx.sv第67行的XOR操作"，整个过程AI自主完成了信号路径追踪、逐bit解码、交叉验证。**我全程没有打开Verdi GUI。**

但请注意——AI之所以能做到这些，是因为skill里预先定义了完整的工作流：怎么读FSDB、信号路径用什么格式、怎么过滤NPI的banner输出、怎么处理时间单位转换。没有这些，AI面对一个FSDB文件是无从下手的。

而我之所以能在AI给出分析后迅速确认结果是对的——"是的，0x6FC对应的data确实是0x7E，确实是bit[0]翻转"——靠的是自己对UART协议和移位寄存器结构的理解。**如果我自己不懂这些，AI给出的结论我也无法验证。**

### 1.4.5 关于这个case的难度

肯定有朋友会说：这case太简单了，直接review RTL就能发现，用不着看波形。

没错。但你敢说你没见过比这还简单的问题花了你半小时？环境少source了一个文件、regression 200个case里25个是同一个误报pattern——这些事每一件都不难，但每一件都在吃你的时间。

**省了十分钟也是省时间。** AI帮你扫掉琐碎的，你就有更多完整的时间块去啃真正的硬骨头。

---

## 1.5 黑猫白猫，能抓老鼠的就是好猫

我坚定地认为，**现在AI的能力仍然被严重低估了。** 大多数人还没找到正确的使用方式。

**AI是杠杆，放大你已有的能力。** 你功底扎实，写出的skill就是高质量的，AI就能发挥巨大效能。你基本功不行，写出了有问题的skill自己也发现不了。

> **AI的所有产出都需要人来review。人review AI结果的带宽，才是AI对一个人提升的真正上限。**

---

## 1.6 关于fsdb\_reader.py

这个脚本值得单独说一下，因为它是整个skill里最有技术含量的部分，也是让AI能够"不开GUI就能看波形"的关键。

FSDB是Synopsys的私有波形格式，没有公开的spec。要读它，要么开Verdi GUI，要么用Synopsys的命令行工具（fsdbreport、fsdb2vcd），要么用他们的NPI（Native Programming Interface）。

`fsdb_reader.py`的策略是**NPI优先，CLI兜底**：

**NPI模式：** NPI是Synopsys提供的C/Python接口，可以在代码里直接打开FSDB、遍历信号、读取值变化。但有一个大坑——NPI的`.so`文件是跟Verdi自带的Python 3.6编译的，如果用系统Python 3.7+去加载，直接segfault，没有任何有用的错误信息。我的脚本自动检测Verdi的bundled Python路径，设置好`LD_LIBRARY_PATH`和`PYTHONPATH`，用subprocess调起来。用户（包括AI）完全不需要关心这些兼容性问题。

**CLI兜底：** 如果NPI不可用（比如Verdi版本太老），自动切换到`fsdbreport`/`fsdb2vcd`命令行工具。功能没NPI全面，但基本的信号读取没问题。

另外还做了一件事：**过滤NPI的banner输出**。每次调NPI，Synopsys都会打印一大段license和版权声明（十几行），混在有用的输出里。脚本里有一个关键词过滤列表，把这些banner行全部干掉，只留下干净的信号数据。这对AI的输出解析至关重要——AI需要结构化的数据，不是license声明。

---

## 1.7 Skill的价值：把"可重复的经验"变成"可执行的知识"

干了这些年，我有一个强烈的感受——很多经验是"做过就会，不做就忘"的。比如怎么把老项目跑起来、`-unit_timescale`和`-timescale`的区别、FSDB的NPI接口怎么调、sim.log里哪些是误报……这些东西写在笔记本里，下次用的时候还得翻半天。

Skill把这些经验变成了AI可以直接执行的指令。下次我换一个项目，只要环境类似，source一下.envrc，AI就能按skill走完整个流程。我不需要再记那些flag，不需要再翻笔记。

更重要的是，这个skill可以分享。团队里的新人、或者任何用类似VCS/Verdi环境的人，装上这个skill就能获得一套即开即用的仿真工作流。AI成了一个"随身携带的、有经验的同事"。

---

## 1.8 写在最后

做IC验证是一份需要耐心和经验的工作。AI不会替代这份工作的核心——理解spec、设计测试策略、分析corner case——这些仍然需要工程师的判断力。

但那些重复性的操作——配环境、跑仿真、分析日志、追踪信号——完全可以交给AI来做。前提是你得把你的经验"教"给它。

Skill就是这个"教"的过程。而你教的质量，取决于你自己的功力。

当然，按照AI进化的速度，那些"硬骨头"迟早也会被啃动。到那个时候，游戏规则又会变。但那是以后的事——眼下，先把能用的工具用好。

黑猫白猫，能抓老鼠的就是好猫。工具不分高低贵贱，能帮你抓到bug的就是好工具。

代码已开源：https://github.com/cike128/eda-sim

如果你也在做数字IC设计，也在探索AI辅助工作流，欢迎交流。

---

*（本文提到的eda-sim skill基于Synopsys VCS/Verdi工具链。Skill运行在Claude Code Opus 4.6 effort max。）*

---

# 2 关于控制Agent上下文的一些方法与思考

> 来源：https://mp.weixin.qq.com/s/UKEy0kFSoLzxq0etvdAqIA
> 作者：AAA建材批发王哥
> update 2026/08/16 01 : 14
---

## 2.1 长对话里 AI 会变笨，这不是错觉

用过 Claude Code 或 Codex 跑过几小时长会话的人，大概都有这个体感：聊到一定程度，AI 像换了个人，开始忘事、开始绕弯、开始把你早就否决过的方案重新提出来。

这不是错觉。**即使是当下最新的御三家 AI——GPT-5.5、Claude Opus 4.7、Gemini 3.1 Pro——上下文占用一旦上到 60%~70% 这个区间，注意力稀释的问题就会客观出现**。这不是某一家模型特有的问题，也不是模型不够强，而是 attention 机制在长序列下的物理限制，不是靠堆参数能根治的。

Stanford 那个 *lost in the middle* 的发现在过去几年里被反复验证：长上下文里开头和结尾的信息记得清楚，中间段的信息准确率掉得最厉害。各家厂商宣称的256k, 1M甚至更大的上下文窗口，跟它实际能稳定使用的"有效上下文"完全是两件事。

放到 coding agent 场景下，这个问题被进一步放大，原因有三个：

1. **上下文是只进不出的**。每次 read file、每个 grep 结果、每条 bash 输出都堆进窗口里，直到你手动 compact 或撞上限。
2. **干扰项密度高**。代码库里语义相似的函数一抓一大把，模型注意力被稀释。
3. **任务跨度长**。一个调试任务跑一两小时是常态，期间上下文质量持续滑坡。

所以问题从来不在于窗口够不够大——基本上你想用多大都有——而在于**怎么让窗口里始终是高质量的信息**。

---

## 2.2 Compact 不是好答案，Sub-agent 才是

绝大多数人遇到这种情况的第一反应是 `/compact`——把历史对话压成摘要，腾出空间继续聊。

这是个无奈的选择。Compact 本质上是一次有损压缩，而且是**模型自己决定哪些信息可以扔**。对于一个长链条的调试任务，被扔掉的往往就是后来被证明关键的那一行约束、那一个 corner case、那一段 waveform 描述。我个人现在已经基本不主动 compact，宁可换思路。

更好的思路是：**从源头控制什么信息有资格进主上下文**。

这就是 sub-agent 真正在做的事。它的本质不是"帮你跑个支线任务"，而是建立了一个**信息隔离边界**——子 agent 在自己独立的上下文窗口里折腾，跑工具、读文件、试错、走弯路，所有这些过程性的、低信噪比的 token 都待在子上下文里，**只有最后被它自己提炼出来的结论，才有资格回到主线**。

主对话从头到尾保持干净。

具体到 Claude Code 和 Codex 这两个工具，我现在最常用的有两套机制，一套轻量、一套重量，分别对应不同场景。

---

## 2.3 轻量版：Claude Code 的 `/btw` 和 Codex 的 `/side`

这是我使用频率最高的一对命令，专门用来处理"快问快答"式的临时插队问题。

什么场景？——主线讨论 RTL 实现，中间冒出一个我不熟悉的概念、一个想确认的细节、一个突然想起来的疑问。这种问题不值得为它专门派一个 sub-agent 跑流程，但又必须搞清楚才能继续往下走。

以前怎么办？要么开个新窗口去问，要么硬着头皮在主线里问完——前者切上下文累，后者污染主线。

`/btw`（Claude Code，by the way 的缩写）和 `/side`（Codex）就是为这个场景设计的：

- 直接在主对话里输入命令，起一个临时的轻量级会话
- 你可以在里面**进行多轮对话**，把这个小问题真正讨论清楚——不是只能问一句就走
- 讨论完直接退出，主线一个字都不被污染，继续从你刚才停下的地方往下走

它比 `/fork` 轻很多，因为不继承父对话的完整历史，起来快、退得也快。我自己日常用它的频率比 `/fork` 还高——基本上每天几十次。

一旦用顺了，你会发现自己的提问意愿都变了：以前嫌切上下文麻烦，很多疑问就咽下去了；现在每个小疑问都可以"快问快答"地解掉，做完一个复杂任务回头看，中途其实顺手把好几个知识盲点也补了。

---

## 2.4 重量版：Claude Code 的 `/fork` 和 Codex 的 sub-agent

`/btw` 和 `/side` 适合小问题，但有些任务本身就复杂——比如"基于我们刚刚定的接口约定，去看看 RX 那边的状态机会不会冲突"——这种任务需要主线积累的完整上下文，需要跑工具、需要可能几十轮的内部探索。这时候轻量级会话就不够用了。

### 2.4.1 Claude Code：`/fork` vs 普通 sub-agent

Claude Code 现在有两套机制，区别值得搞清楚：

- **普通 sub-agent**：你让它"派一个 agent 去把 testbench 里所有的 assertion failure 列出来"，它会启一个干净上下文的子 agent，跑完只回传结论。适合任务独立、不需要主线背景的场景。
- **`/fork`**：这是相对新的机制，关键差别在于——**fork 出来的子 agent 继承父对话的完整历史和系统提示**，但它自己后续的工具调用不污染主上下文。Anthropic 官方文档里写得很清楚：fork 牺牲了输入侧的隔离，换取了"不用重新解释背景"这件事，同时保留输出侧的隔离。

什么时候用哪个，我自己的判断标准很简单：

- 如果支线任务**需要主线积累的上下文**，用 `/fork`。
- 如果支线任务**完全独立**（比如"去 grep 一下整个 repo 里所有用到 SVA 的地方"），用普通 sub-agent，启一个干净窗口反而更好。

启用 `/fork` 需要在环境变量里设 `CLAUDE_CODE_FORK_SUBAGENT=1`，然后在交互会话里直接 `/fork <你的问题>` 即可。

### 2.4.2 Codex CLI：显式委派 + `/agent` 切换

Codex 这边的设计哲学不太一样。它没有对应的 `/fork` 命令，而是：

- 你**显式地**在 prompt 里说"开一个 subagent 去做 X"，Codex 才会派生子线程（默认是不会的）。
- 用 `/subagents` 命令在多个活跃 agent thread 之间来回切换、查看、停止、关闭。
- 主线和子线之间是平行的多线程模型，而不是 Claude Code 那种"父-子层级"模型。

Codex 官方 best practice 文档里有句话写得挺直白：**主 agent 应该聚焦核心问题，subagent 用来 offload 探索、测试、triage 这种"边界明确但会污染上下文"的工作**。

---

## 2.5 为什么这套用法对我个人特别契合

这一段我想多说几句。

我自己的思维方式比较发散，做事喜欢刨根问底。一个新概念出现，没搞懂之前会一直停在那里。这个习惯在没有 sub-agent 之前其实挺折磨人——讨论一个 RTL 实现，中间冒出一个我不熟悉的 protocol 细节，我要么硬着头皮跳过去（后面经常翻车），要么停下来去查，等查完回来，主线的思路已经断了，得花十几分钟把刚才在想什么重新捡起来。

`/btw`、`/side`、`/fork`、sub-agent 这一整套机制，相当于让我能在主线里随时按下"暂存"——派一个支线去把疑问搞清楚——再回到主线，主线的所有状态原封不动。我在上面感受到的认知负担减轻是非常显著的，做复杂项目的时候明显感觉自己能同时 hold 住更多东西了。

以前 Claude Code 早期版本只有 `/branch`，体验差一截。Branch 出去要回来还得记 session ID、还得手动 resume，这个流程本身就在消耗注意力，几次之后你就懒得用了。现在两个工具都把这套流程做顺了，门槛低到你不会再因为"嫌麻烦"而放弃使用，这才是它们在 2026 年初我觉得最值得推荐的更新。

---

## 2.6 最后一点感受

写到这里，顺便想多说几句最近这几个月深度使用 Agent CLI 工具下来的一点感受。

用得越久，我越发现一件事：**在有限的资源里，让一个能力有限的下属把事情做成，真的是一门难度不低的手艺**。

现在的 AI 模型，哪怕已经迭代到 GPT-5.5、Claude Opus 4.7 这一代，能力依然是有边界的。它会忘事、会过度自信、会在不该走捷径的地方走捷径、会在你没盯着的时候偷偷把硬骨头绕过去。

面对这种情况，我现在已经不会主动去要求模型自己 auto-complete 一个长任务，也不会傻等它"自己想明白"——因为那样基本一定会损失一些东西，而且损失的往往是你最在意的那部分。

我现在的做法是：**有意识地去控制工具的"行为"，而不是去期待它的"能力"强到能解决所有事情**。

我不去强求它变得更聪明——它的智商就在那里，我改变不了；但我可以通过 `/btw`、`/side`、`/fork`、sub-agent 这些机制，把它框定在一个我已经验证过、效果最好的工作状态里。让主线干主线该干的事，让支线吸收所有探索性的损耗。

这就是我现在用 AI 的一点态度。仅供参考。

如果您有什么关于数字设计前端，中端，验证，任何与 AI 相关的想法、建议，或者有一些想要去实现的东西，欢迎来聊一聊。

![](AI在芯片中的实际应用_assets/image-0006.jpg)

---

# 3. AI辅助覆盖率收敛的坑在哪里？

> 来源：https://mp.weixin.qq.com/s/yvwsNtz5eaWrZLzUVAyYMg
> 作者：make ic
> update 2026/08/17 23 : 20

![](AI在芯片中的实际应用_assets/image-0007.png)

---

验证要回答的问题是：**设计的行为空间里，有没有和意图不一致的地方。**

而覆盖率回答的是另一个问题：**已经跑过的激励，碰到了哪些预先定义好的点。**

这两个问题之间，隔着两层塌方。

第一层，覆盖模型是人写的。没写进 covergroup 的状态组合，工具永远不会提醒它缺席。

第二层，检查器也是人写的。约束随机跑到了那个点，scoreboard 没盯住那条路径，覆盖率照样加一分，bug 照样活着。

所以"覆盖率 100%"这句话，严格的说法应该是：*人类想到的地方，都跑过了。*

至于人类没想到的部分——那才是流片失败的主要来源。

---

## 3.1 机器学习进来了，先看清它在哪一层干活

近两年 EDA 厂商把 AI 塞进了验证流程，方向基本一致：用机器学习引导约束随机，把仿真算力花在有价值的种子上。

Synopsys 的 VSO.ai 在 VCS 里做覆盖驱动的约束求解，宣传的数据是同样的跑测次数下多拿到 33% 的功能覆盖率，同时缩小回归规模。Cadence 的 Verisium SimAI 走的是从回归数据里学习、把仿真往罕见事件上推的路子。

这些工具确实有用——**它们把"最后 10% 覆盖率要烧掉 60% 算力"这件事，变得便宜了。**

但要说清一件事：这一类优化，做的是**搜索效率**，而不是**认知补全**。

模型的输入是覆盖点定义、约束、历史仿真数据。它的目标函数是"点亮更多已定义的 bin"。**一个从未被定义的覆盖点，对模型来说不存在梯度，也不存在诱因。**

指望它替人想出漏掉的场景，等于指望地图软件帮你发现没修的路。

再补一句更扫兴的：机器学习天生偏爱"接近已跑通路径"的解。**它会把已经收敛的区域打磨得更漂亮，而真正的盲区往往在分布的尾巴上**，那里样本少、回报慢，模型本能地绕开。

覆盖率曲线因此变得更好看，风险曲线未必跟着下降。

---

## 3.2 真正能预测盲区的信号，藏在别的地方

如果目标是找盲区，机器学习的输入就该换一批数据。有几个方向在实践里明显更有价值。

**代码结构与覆盖率的错配。** 拿 RTL 的控制流复杂度、状态机深度、跨时钟域路径数量，去对齐功能覆盖模型的密度。某个模块代码复杂度排前 10%，覆盖点数量排后 30%——这个偏差本身就是一条高质量线索。

**历史 bug 的空间分布。** 同一家公司、同一批人、同一套设计习惯，bug 会聚类。上一代芯片的 bug 集中在低功耗握手和复位释放边界，这一代大概率还在那里。**Bug 有惯性，因为写代码的人有惯性。**

**回归数据里的"永远相邻"关系。** 两个覆盖点在几十万次仿真里从来没有同时被点亮，可从设计意图看它们应该可以共存。这种交叉的缺口，人工翻报告基本翻不出来，做成关联分析却很自然。

---

## 3.3 工具变强之后，判断力反而更贵

工具能把搜索做到极致，唯独没法替人回答"什么才值得被搜索"。**覆盖率盲区的本质是认知盲区，而认知盲区不在仿真器里，在人的假设里。**

一个成熟的做法：把每一条 waiver 当作一次赌注登记下来，写清楚赌的是什么前提。项目末期回头看这份清单，往往比任何 AI 报告都更有信息量——它记录的是团队集体相信过、却没验证过的东西。

---

覆盖率是一面镜子。它照出的是验证工程师的想象力边界，而不是设计的正确性边界。

机器学习让这面镜子擦得更亮、更快。至于镜子对着的方向，还得靠人自己拿主意。

---

# 4. AI 写 UVC？我踩了无数坑后，搞了个 Skill 包救自己

> 来源：https://mp.weixin.qq.com/s/JPJMb_WtbNge857dP66kCg
> 作者：福尔摩芯
> update 2026/08/22 11 : 21

![](AI在芯片中的实际应用_assets/image-0008.png)

## 4.1 一周没更新，后台炸了

上周休假出去浪了一圈，整整一个星期没更新公众号。本来以为大家会把我忘了，结果后台私信和互动反而更多了。

看来大家对 UVM/UVC 的开发确实很感兴趣，这个话题的热度远超我的预期。

本来我在想，怎么能把 UVC 的开发介绍做得更浅显易懂一些。但是转念一想——现在都是 AI 时代了，**未来的 UVC 开发，完全可以交给 AI 来做啊！**

于是旅游回来以后，我就迫不及待地尝试了一下。

结果……一言难尽。

---

## 4.2 第一次尝试：AI 的 UVM，语法都写不对

我先用 mimo-v2.5-pro，让它按照我之前介绍的 UVC 开发思路，去开发一个 APB UVC。

信心满满地等它输出，然后一看代码，我人傻了：

```
// AI 生成的"精彩"代码
// AI 生成的"精彩"代码
class
class
 apb_driver
extends
extends
 uvm_driver;
    def run_phase(self, phase):
// 这是 Python 吧？！
// 这是 Python 吧？！
while
while
 True:
            item = self
.seq_item_port
.seq_item_port
.get_next_item
.get_next_item
()
            self
.drive
.drive
(item)
function
function
 drive(item):
// SV 里混 Python？
// SV 里混 Python？
if
if
 item
.direction
.direction
 ==
"READ"
"READ"
:
            self
.vif
.vif
.paddr
.paddr
 = item
.addr
.addr
// ...
// ...
```

**我：？？？这是 UVM？**

UVM 里混杂了 Python 的 `def`、`self`、`while True`，还有一堆不伦不类的语法。这要是能仿真通过，我把键盘吃了。

不死心，我又切到了 DeepSeek v4，结果也没好到哪里去。虽然语法错误少了，但生成的代码风格诡异，各种反模式层出不穷。

**原因分析：** 用于 AI 训练的 SystemVerilog/UVM 代码量，相比 Python、Java 这些主流语言，实在是太少了。AI 对 UVM 的理解，基本停留在"看起来像 UVM"的程度。

---

## 4.3 第一轮突破：给 AI 加上"红绿灯"——TDD

既然 AI 写的代码质量不行，那怎么才能让它**知道自己写错了**？

答案是：**测试！**

我让 AI 先写了一套 APB 的基本测试用例：

```
// 基础测试用例
// 基础测试用例
class
class
 apb_basic_test
extends
extends
 uvm_test;
// 1. 单次读操作
// 1. 单次读操作
// 2. 单次写操作
// 2. 单次写操作
// 3. 随机读写操作
// 3. 随机读写操作
// 4. 连续读（back-to-back read）
// 4. 连续读（back-to-back read）
// 5. 连续写（back-to-back write）
// 5. 连续写（back-to-back write）
// 6. 读写交替操作
// 6. 读写交替操作
endclass
endclass
```

然后告诉 AI："你按照 TDD 的方式开发 UVC，写完以后必须用仿真工具跑通这些测试，才算完成。"

这一步效果立竿见影！

AI 开发的 UVC 终于能编译通过、仿真成功了。但是……实际操作的时序还是有些问题。比如 APB 的 setup phase 和 access phase 时序不对，信号的驱动时机有偏差。

于是又继续来回迭代，让 AI 通过查看仿真波形（VCD）来识别自己的时序错误，然后修复。

**折腾了好几轮，UVC 终于能跑起来了。**

---

## 4.4 第二轮突破：Wavedrom——让 AI 正确"看懂"时序

时序问题一直困扰着我。

做芯片设计或验证的同学都知道，**描述时序是一件非常麻烦的事情**。尽管我们可以用自然语言描述，但要把一个时序描述清楚，自然语言的篇幅可能比 Verilog 代码还长。

比如你想告诉 AI："在时钟上升沿，如果 PSEL 为高且 PENABLE 为低，则进入 setup phase，此时 PADDR 和 PWRITE 必须稳定；下一个周期 PENABLE 拉高，进入 access phase……"

光描述一个 APB 的基本读写时序，就要写一大段。而且 AI 还不一定能完全理解。

**有没有一种好的交互格式，能让 AI 准确地知道各信号之间的时序关系？**

这时候我发现了一个叫 **WaveDrom** 的东西。

> “
>
> **WaveDrom** 是一个基于 JSON 格式描述数字时序图的开源工具。它使用一种叫做 WaveJSON 的语法，可以用简洁的 JSON 结构精确描述信号的波形时序。
>
> 官网：https://wavedrom.com/

举个例子，用 WaveDrom 描述一个简单的 APB 读操作：

```
{
"signal"
"signal"
: [
    {
"name"
"name"
:
"PCLK"
"PCLK"
,
"wave"
"wave"
:
"p....."
"p....."
},
    {
"name"
"name"
:
"PSEL"
"PSEL"
,
"wave"
"wave"
:
"01.0.."
"01.0.."
},
    {
"name"
"name"
:
"PENABLE"
"PENABLE"
,
"wave"
"wave"
:
"0.10.."
"0.10.."
},
    {
"name"
"name"
:
"PWRITE"
"PWRITE"
,
"wave"
"wave"
:
"0....."
"0....."
},
    {
"name"
"name"
:
"PADDR"
"PADDR"
,
"wave"
"wave"
:
"x=x..."
"x=x..."
,
"data"
"data"
: [
"addr"
"addr"
]},
    {
"name"
"name"
:
"PRDATA"
"PRDATA"
,
"wave"
"wave"
:
"x.=x.."
"x.=x.."
,
"data"
"data"
: [
"data"
"data"
]},
    {
"name"
"name"
:
"PREADY"
"PREADY"
,
"wave"
"wave"
:
"x.10.."
"x.10.."
}
  ],
"config"
"config"
: {
"hscale"
"hscale"
:
2
2
}
}
```

用这种格式，我可以把时序**精确地、结构化地**告诉 AI，而不是用一大段自然语言去描述。

![](AI在芯片中的实际应用_assets/image-0009.png)

时序图效果

AI 拿到这个 JSON 以后，就能清楚地知道：

- 什么时候 PSEL 拉高
- 什么时候 PENABLE 拉高
- 什么时候地址和数据必须稳定
- 什么时候从设备可以返回数据

**继续迭代 UVC，此时生成的代码时序已经比较准确了。**

---

## 4.5 第三轮挑战：Guidelines——让 AI 写出"好"的 UVC

UVC 能跑、时序也对了，但这还不够。

UVC 毕竟是给别人用的，需要满足一些工程规范：

- **高内聚、低耦合**：组件之间职责清晰
- **依赖注入原则**：配置通过顶层注入，而不是底层自己去 config\_db 里 get
- **避免递归式 config\_db**：不要到处乱 set/get，导致配置链路混乱
- **遵循 UVM Cookbook 的最佳实践**

于是我把 UVM Cookbook 里的 guideline 做成 skill，用来指导 AI 生成 UVC。

**然后……我就被拖进泥潭了。**

### 4.5.1 问题一：Guideline 写得太抽象，AI 不理解

一开始我写的 guideline 比较简短：

> “
>
> "代码要符合 OOP 思想，遵循高内聚低耦合原则。"

结果 AI 根本不买账。它"理解"的 OOP 跟我想要的完全不一样。

后来我发现，**必须写成"正确示范"和"错误示范"的形式**，AI 才能理解：

```
// ✅ DO: Use config to control active/passive
// ✅ DO: Use config to control active/passive
if
if
 (cfg
.is_active
.is_active
 == UVM_ACTIVE)
begin
begin
    drv = my_driver::type_id::create(
"drv"
"drv"
,
this
this
);
end
end
// ❌ DON'T: Use config_db for config within UVM hierarchy
// ❌ DON'T: Use config_db for config within UVM hierarchy
uvm_config_db
#(int)::get(this, "", "is_active", is_active)
#(int)::get(this, "", "is_active", is_active)
;
// WRONG
// WRONG
```

但是这样一来，skill 的内容就变得非常大，很快就把上下文窗口占满了。UVC 迭代两轮，上下文就没了。

### 4.5.2 问题二：模型差异巨大

不同模型对代码的理解能力天差地别：

| 模型 | 表现 |
| --- | --- |
| GPT-5.5 | 一直比较符合预期，理解能力强 |
| MIMO v2.5 | 偶尔降智，需要反复提醒 |
| DeepSeek v4 | 时好时坏，不太稳定 |

切换模型以后，之前好不容易调通的流程又开始出问题。

**这一刻我深刻体会到：光靠 prompt 和 guideline，是不够的。**

---

## 4.6 最终方案：代码模板化，脚本 + Prompt 两步走

折腾了好几天以后，我终于想通了一个道理：

> “
>
> **不要让 AI 做它不擅长的事情。**

AI 擅长什么？**填充逻辑、实现细节、处理变化的部分。**

AI 不擅长什么？**保证代码结构的一致性、遵循复杂的规范。**

那怎么办？

**脚本生成框架，AI 填充逻辑。**

以前我不是开发过一个生成 UVC 的脚本吗？（uvc\_gen）先把 UVC 的框架用脚本生成出来，再让 AI 去填充时序和具体的协议逻辑。

**说干就干！**

效果立竿见影——AI 生产出来的 UVC 非常符合我的预期了：

```
#
#
 第一步：脚本生成 UVC 框架
 第一步：脚本生成 UVC 框架
python3 uvc_gen.py -n apb -m single -v v1.0 -o ./my_project
#
#
 第二步：AI 填充时序和逻辑
 第二步：AI 填充时序和逻辑
#
#
 此时 AI 只需要关注 drive_trans() 和 rcv_data_phase() 的实现
 此时 AI 只需要关注 drive_trans() 和 rcv_data_phase() 的实现
```

脚本保证了代码结构、命名规范、TLM 连接等"骨架"部分的正确性，AI 只需要往里面填"肉"。

---

## 4.7 env-builder：一个完整的 UVC 开发 Skill

搞定了上面这些问题以后，我把整个流程整合成了一个 Claude Code Skill——**env-builder**。

这个 Skill 由两部分组成：

### 4.7.1 uvc\_gen：UVC 框架生成脚本

uvc\_gen 是一个 Python 脚本，支持生成常见的 UVM 组件框架：

**支持的模式：**

- **single 模式**：单 agent 类型（如 APB、SPI、I2C）
- **mstslv 模式**：主从类型（如 AXI、AHB）
- **多通道 env 层**：支持多个 agent 的环境封装

**设计原则：**

- 遵循**依赖注入**原则：UVC 开发后向使用者提供 `sequence_lib` 作为 API
- 控制 agent 只能通过顶层的 cfg 依赖注入
- 去掉了递归式的 `config_db`，避免底层配置被破坏
- 最终使用体验：**把 UVC 当作 Python 的库一样简洁，最大化方便使用者集成和复用**

```
# 安装
# 安装
npx skills add HolmeXin2630/ic-verifier -g
# 使用（自动调用 uvc_gen）
# 使用（自动调用 uvc_gen）
> /env-builder
> Create an APB UVC with driver, monitor, and sequencer
```

### 4.7.2 开发 UVC 的流程

参照 **Brainstorm** 的思路，开发 UVC 的过程中 AI 会层层询问你的需求：

```
┌─────────────────────────────────────────────────────────┐
│  Step 1: 需求澄清                                        │
│  - 这是什么协议？                                        │
│  - 需要哪些组件？（driver/monitor/scoreboard）            │
│  - 使用场景是什么？                                      │
│  - 时序要求是什么？                                      │
├─────────────────────────────────────────────────────────┤
│  Step 2: 生成 UVC 框架                                   │
│  - 调用 uvc_gen 生成代码骨架                             │
│  - 生成 config、transaction、agent 等组件                │
├─────────────────────────────────────────────────────────┤
│  Step 3: 编写开发文档                                    │
│  - 输出 spec（架构、接口、时序）                         │
│  - 用户确认后继续                                        │
├─────────────────────────────────────────────────────────┤
│  Step 4: TDD 开发                                        │
│  - 先写测试用例（RED）                                   │
│  - 填充实现逻辑（GREEN）                                 │
│  - 仿真验证（需要告知 AI 你的 EDA 工具）                 │
├─────────────────────────────────────────────────────────┤
│  Step 5: Review                                          │
│  - 调用 review-agent 审查代码                            │
│  - 检查 UVM 规范、命名、时序等                           │
│  - 发现问题则回到 Step 4                                 │
└─────────────────────────────────────────────────────────┘
```

在 TDD 开发过程中，你需要告知 AI 你所使用的 EDA 工具是什么（VCS、Xcelium、Questa 等），它会自动配置编译和仿真命令。

![](AI在芯片中的实际应用_assets/image-0010.png)

---

## 4.8 ic-verifier：一个面向 DV 的 AI Skill 技能包

env-builder 只是开始。

我的规划是开发一个完整的 **IC Verifier** 技能包，覆盖验证工程师的日常工作场景：

| Skill | 命令 | 状态 | 说明 |
| --- | --- | --- | --- |
| UVM Environment Builder | `/env-builder` | ✅ 可用 | UVC/VIP 开发全流程 |
| Testplan Manager | `/testplan` | 🔜 规划中 | 测试计划管理 |
| Coverage Closure | `/coverage` | 🔜 规划中 | 覆盖率收敛 |
| Formal Property | `/formal` | 🔜 规划中 | 形式化属性验证 |

**最终目标：** 让 AI 成为验证工程师的得力助手，而不是一个"看起来像在帮忙，实际上在添乱"的工具。

---

## 4.9 总结：AI 开发 UVC 的正确姿势

经过这一番折腾，我总结出了 AI 开发 UVC 的几个关键点：

### 4.9.1 TDD 是必须的

不要相信 AI 一次就能写对。先写测试，再写代码，让仿真工具来验证。

### 4.9.2 时序描述要结构化

用 WaveDrom 或类似的格式精确描述时序，而不是用自然语言"大概说一下"。

### 4.9.3 框架和逻辑分离

用脚本生成框架（保证结构正确），让 AI 只负责填充逻辑（发挥它的创造力）。

### 4.9.4 Guidelines 要具体

"符合 OOP 思想"这种抽象描述没用，必须写成"正确示范 vs 错误示范"的形式。

### 4.9.5 不同模型要区别对待

GPT-5.5、MIMO、DeepSeek 的表现差异很大，要根据实际情况调整策略。

---

## 4.10 项目地址

**ic-verifier：** https://github.com/HolmeXin2630/ic-verifier

欢迎安装试用：

```
npx skills add HolmeXin2630/ic-verifier -g
```

**欢迎大家使用，有任何问题和建议，欢迎在 GitHub 上提 issue！**

---

#UVM #VIP #UVC #AI #SKILL

---

# 5. AI 会写 testbench 了，验证工程师慌不慌？

> 来源：https://mp.weixin.qq.com/s/TcqJtzm75mTMgOTU_HJYWQ
> 作者：make ic
> update 2026/08/22 11 : 44

某个周三下午，组里的新人用大模型生成了一份 UVM 环境的骨架。

![](AI在芯片中的实际应用_assets/image-0011.png)

driver、monitor、sequencer、agent，一应俱全，命名规范得能直接进公司模板库。二十分钟，抵得上过去两天的手速。

---

## 5.1 先说清楚 AI 到底吃掉了什么

得承认，AI 在验证上的进展是真的，而且不慢。

Synopsys 的 VSO.ai 已经在 NVIDIA 的项目里落地，公开数据是功能覆盖率提升超过 33%，回归压缩 2 到 7 倍。Cadence 在 2026 年 COMPUTEX 上直接放出了所谓"L5 级自主 AI 芯片设计工程师"。学术界那边，ASP-DAC 2026 有专门的 LLM 辅助电路验证综述，agentic AI 做形式验证覆盖率收敛的论文也已经出来了。

把这些东西拆开看，AI 真正吃掉的是三类活：

**「重复性的代码搬运」**——寄存器模型、总线 agent、这些有标准答案的东西。

**「大规模的搜索」**——从几千个随机种子里挑出最有可能碰到新覆盖点的那批，这本来就是机器擅长的组合优化问题。

**「日志里的模式识别」**——一万条 failure 归成三类，找出最可能的 root cause 嫌疑犯。

这三类加起来，占了不少验证工程师的日常工时。有人焦虑，很正常。

---

## 5.2 验证到底在解什么问题

想找护城河，得先回到最原始的问题上。

**「验证的本质，是在一个近乎无限大的状态空间里，用有限的资源，建立起"这颗芯片能用"的信心。」**

注意这句话里的三个词：无限、有限、信心。

状态空间是无限的。一个带 cache 的多核 SoC，理论状态数远超宇宙原子总数，穷举永远不可能。资源是有限的，流片有 deadline，服务器有配额，人有下班时间。

而"信心"这个词，是整件事的关键——它不是一个数字，它是一个判断。

覆盖率 100% 不等于设计没问题，这话每个验证工程师都会背。真正的问题在于：**「覆盖率模型本身是人定义的。」** 一个没被写进 covergroup 的场景，跑一亿个种子也不会被发现。

AI 能帮你更快填满你画的那张表。**「它填不了你没画的那一格。」**

---

## 5.3 护城河在哪：三样 AI 短期拿不走的东西

### 5.3.1 把模糊的意图翻译成可执行的判断

Spec 永远是不完整的。这不是架构师偷懒，是自然语言的固有属性。

真正决定芯片成败的，往往是那些没写进文档的东西：两份 spec 之间互相矛盾的一句话、设计者脑子里默认成立但从没说出口的假设。

处理这些，靠的是把设计、软件、系统架构三边的人拉进一个会议室，吵一个下午，然后达成共识。**「这个过程的输入不在任何一份可训练的语料里。」**

### 5.3.2 判断什么值得验，什么不值得

带宽有限时的取舍，是验证工程师最值钱的能力，也最不容易被看见。

流片前两周，还剩 40 个覆盖点没收敛。哪几个必须补，哪几个可以写进 waiver，哪几个属于"理论上存在但实际软件永远不会这么用"——**「做这个决定要承担责任，而 AI 目前不承担任何责任。」**

工具可以给你一份排序建议。签字的还是人。

### 5.3.3 对失败的直觉

老验证工程师有一种说不清的能力：看一眼波形，就知道"这个味道不对"。

这个直觉的底层，是几百个 bug 积累下来的因果模型——知道哪种设计写法容易在什么地方出错，知道哪个 IP 的 vendor 交付质量向来一般，知道上次流片回来的 silicon bug 长什么样。

**「这是经验的压缩，不是知识的检索。」** 大模型能检索，压缩得还很粗糙。

护城河存在，但它在变窄。

那种只会照着 spec 写 sequence、只会盯着覆盖率报告跑回归的岗位，会消失。速度可能比预期快。

行业里已经有一种明显的分层：**「AI 让能力强的人更强，让能力一般的人被替代。」** 高级工程师用 AI 一天干完三天的活，因为他知道该让 AI 干什么、以及怎么检查 AI 干的对不对。而缺乏系统理解的人，用 AI 生成一堆自己看不懂的代码，反而制造了更难查的问题。

**「AI 放大的是判断力，不是打字速度。」** 判断力为零的时候，任何数乘以零还是零。

---

## 5.4 该往哪走

三个方向，说得直接一些。

**「往上走」**，走向系统。理解这颗芯片上跑的软件长什么样，理解 SoC 级的性能瓶颈在哪，理解客户真正会怎么用它。这一层的上下文，AI 目前接不住。

**「往下走」**，走向底层机制。形式验证的收敛原理、时序与功能的交界处、post-silicon 的 debug。这些地方需要的是对因果链的完整掌握。

**「往旁边走」**，成为把 AI 用到极致的那个人。工具链的能力边界在哪、什么时候该信它、什么时候必须自己动手，这本身正在成为一项独立的专业技能。

---

## 5.5 最后

验证这个行当，从来卖的都不是代码行数。

**「卖的是一句话：这颗芯片，可以流片了。」**

说这句话需要的东西——对复杂系统的理解、对未知风险的敬畏、以及愿意为判断承担后果——这些东西，机器暂时还学不会。

至于"暂时"有多长，没人知道。所以更实际的做法是：**「别把时间花在担心上，花在那些让判断力变得更值钱的事情上。」**

毕竟，AI 现在还不会在流片评审会上举手说"这个我签"。

---
